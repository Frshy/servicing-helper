version: '3'
services:
  auth-microservice:
    container_name: gateway-microservice-container
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3005:3005"
    volumes:
      - ".:/usr/src/app"
